# The generated briefs are build output, not source: they are regenerated from
# committed artifacts by `npm run cfb:brief` whenever anyone needs one, and a
# 1 MB PDF per week in git history is a cost with no reader.
*.pdf
*.html
*.csv
