# Set these as Supabase Edge Function secrets, NOT in any committed file:
#   supabase secrets set ODDS_API_KEY=your_rotated_key
#   supabase secrets set ALLOWED_ORIGIN=https://your-domain.com
ODDS_API_KEY=
ALLOWED_ORIGIN=*
