# Supabase Edge Functions are deployed by pasting into the Supabase dashboard,
# not from this repo — the directory was removed from the branch deliberately
# (see "Delete supabase/functions directory"). Ignored so local working copies
# used to prepare a deploy do not get committed back in.
supabase/
